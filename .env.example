# Database Configuration
# MariaDB credentials
DB_ROOT_PASSWORD=your_root_password_here
DB_NAME=spring-boot-kt
DB_USER=your_db_user
DB_PASSWORD=your_db_password_here
DB_PORT=3306

# Spring Boot DataSource Configuration
# For local development (connecting to localhost)
SPRING_DATASOURCE_URL=jdbc:mariadb://127.0.0.1:3306/spring-boot-kt

# Application Configuration
APP_PORT=8081

# Production Configuration (optional, used by docker-compose.prod.yml)
# DATA_DIR=./data
# VERSION=1.0.0
# REGISTRY=local

# Monitoring Configuration (optional, used by docker-compose.monitoring.yml)
# GRAFANA_USER=admin
# GRAFANA_PASSWORD=admin
