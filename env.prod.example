# ============================================
# Production Environment Configuration
# ============================================
# Copy this file to .env.prod and update with actual values
# NEVER commit .env.prod to version control!
#
# Usage:
#   cp env.prod.example .env.prod
#   chmod 600 .env.prod
#   # Edit .env.prod with your values
#   ./deploy-prod.sh
#

# ============================================
# Application Version
# ============================================
VERSION=1.0.0
BUILD_DATE=2024-12-24

# ============================================
# Container Registry
# ============================================
# Leave as 'local' for local builds, or use your registry
REGISTRY=local
# Example: REGISTRY=docker.io/yourcompany
# Example: REGISTRY=your-registry.com:5000/project

# ============================================
# Application Settings
# ============================================
APP_PORT=8081

# ============================================
# Database Configuration
# ============================================
DB_NAME=web_ai
DB_USER=appuser

# IMPORTANT: Use strong passwords!
# Generate with: openssl rand -base64 32
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE
DB_ROOT_PASSWORD=CHANGE_ME_STRONG_ROOT_PASSWORD_HERE

DB_PORT=3306

# ============================================
# Data Directory
# ============================================
# Directory for persistent data storage
DATA_DIR=/var/lib/springboot-ktorm-app

# ============================================
# Monitoring (Optional)
# ============================================
GRAFANA_USER=admin
GRAFANA_PASSWORD=CHANGE_ME_GRAFANA_PASSWORD

# ============================================
# Security Checklist
# ============================================
# Before deploying to production:
# [ ] All passwords have been changed to strong passwords
# [ ] Each service uses a different password
# [ ] Passwords are at least 16 characters long
# [ ] .env.prod has permissions set to 600
# [ ] .env.prod is in .gitignore
# [ ] Domain names are correct
# [ ] SSL certificates are ready
# [ ] Firewall rules are configured
# [ ] Backup strategy is in place
#
# Password generation examples:
#   openssl rand -base64 32
#   pwgen -s 24 1
#   LC_ALL=C tr -dc 'A-Za-z0-9' </dev/urandom | head -c 24

# ============================================
# Notes
# ============================================
# 1. This file contains sensitive information
# 2. Keep it secure and never commit to git
# 3. Backup this file securely
# 4. Rotate passwords regularly
# 5. Use different passwords for different environments

